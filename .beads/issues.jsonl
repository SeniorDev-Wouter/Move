{"_type":"issue","id":"taskflowtest-af0","title":"UI: retro components, App wiring, retro CSS, integration tests","description":"## Goal\nBuild the retro Move UI — the window frame, Clippy-style assistant + reminder bubble, settings/loadout/catalog/tag/progress panels — wire them into `App.tsx` via `useMove` + `useReminderEngine`, apply the late-'90s MS-Office retro styling in `index.css`, and cover the flows with integration tests.\n\n## Context\nFinal assembly ticket. All logic, hooks, and PWA infra are already merged; this ticket only composes React components (named-function components with a local `XProps` type, Tailwind v4 CSS-first + CSS variables) and wires state. `App.tsx`/`App.test.tsx` currently hold a throwaway shell from the foundation ticket — rewrite them fully. Adapt all hook/module imports to their actual merged signatures.\n\n**Design target (carry verbatim to the worker):** the retro late-'90s MS Office / Clippy aesthetic derives from `ai-design/frontendstyle.jpg`. That file is forbidden/gitignored — NEVER open or modify it; build the beveled retro look (window chrome, yellow speech balloon, gray 3D borders) to match it using custom CSS in `src/index.css`.\n\n## Acceptance criteria\n- New components under `src/components/`: `RetroWindow.tsx`, `Assistant.tsx`, `ReminderBubble.tsx`, `SettingsPanel.tsx`, `LoadoutLibrary.tsx`, `ExerciseCatalog.tsx`, `TagRuleEditor.tsx`, `ProgressPanel.tsx`.\n- `src/App.tsx` composes them inside `RetroWindow`, wiring `useMove` + `useReminderEngine`.\n- `src/index.css` keeps `@import 'tailwindcss';` and defines a retro palette + beveled component styling per `ai-design/frontendstyle.jpg`.\n- Reminders START PAUSED with a visible **Start** control; a fresh visitor sees the default \"Office-safe\" loadout.\n- `npm run lint`, `npm run build`, `npm test` pass.\n\n## File changes\nSketches are guidance; adapt to the actual merged hooks/modules.\n\n### `src/components/RetroWindow.tsx` (new)\n```tsx\ntype RetroWindowProps = { title: string; children: React.ReactNode }\n// Beveled window frame + title bar (per ai-design/frontendstyle.jpg).\nexport function RetroWindow({ title, children }: RetroWindowProps) { /* ... */ }\n```\n\n### `src/components/Assistant.tsx` + `src/components/ReminderBubble.tsx` (new)\n```tsx\n// Assistant: the Clippy-style character that always hosts the in-app reminder in a yellow retro\n// speech balloon (per ai-design/frontendstyle.jpg). Shows the ReminderBubble when `current` is set,\n// and the \"no eligible exercises — adjust your loadout\" message when the pool is empty.\ntype ReminderBubbleProps = {\n  exercise: Exercise\n  onAction: (a: ReminderAction) =\u003e void   // Done / Skip / Snooze / Shuffle\n}\n// ReminderBubble renders exercise.image (placeholder), name, formatTarget(target), instructions,\n// and all four action buttons (Done, Skip, Snooze, Shuffle) as accessible \u003cbutton\u003es.\nexport function ReminderBubble({ exercise, onAction }: ReminderBubbleProps) { /* ... */ }\n```\n\n### `src/components/SettingsPanel.tsx` (new)\nInterval + snooze number inputs (positive integers, min 1), the Start/Stop control (calls `engine.start()`/`engine.stop()`), the context selector (lists `context`-axis tags; calls `setContext`), and the equipment checklist (lists `equipment`-axis tags; toggles `ownedEquipment` via `toggleEquipment`).\n\n### `src/components/LoadoutLibrary.tsx` (new)\nList loadouts, show the active one, switch active (`setActiveLoadout`), create/edit (embeds `TagRuleEditor`), export (build a bundle and offer it as a download/text) and import (parse text, call `importBundle`). Surface size/completeness/safety errors from `importBundle` to the user, and render the axis-conflict keep-or-rename choice UI when `importBundle` reports conflicts.\n\n### `src/components/ExerciseCatalog.tsx` (new)\nList exercises; add-custom form (name, instructions, structured target reps|time, image upload validated with `isSafeImage` + `MAX_IMAGE_BYTES`, tags) that calls `addExercise`; mint a tag inline via an explicit **axis picker** (calls `addTag(name, axis)`).\n\n### `src/components/TagRuleEditor.tsx` (new)\nEdit a loadout's include / require-all / exclude tag-name lists; minting a new tag here ALWAYS shows a visible axis picker (calls `addTag(name, axis)`).\n\n### `src/components/ProgressPanel.tsx` (new)\nShow done count, ignored/skipped count, and current-day streak from `deriveStats`. Primary factual line \"Sitting breaks taken: N\" WITH the `Diaz et al., Annals of Internal Medicine, 2017` citation; a secondary \"≈ N×2 active minutes\" line CLEARLY labelled a playful estimate (no citation).\n\n### `src/App.tsx` (rewrite from the foundation shell)\n```tsx\nfunction App() {\n  const move = useMove()\n  const engine = useReminderEngine(/* move.state, move.recordAction, sw registration, ... */)\n  return (\n    \u003cRetroWindow title=\"Move\"\u003e\n      \u003cAssistant current={engine.current} onAction={engine.handleAction} /\u003e\n      \u003cSettingsPanel ... running={engine.running} onStart={engine.start} onStop={engine.stop} /\u003e\n      \u003cLoadoutLibrary ... /\u003e \u003cExerciseCatalog ... /\u003e \u003cProgressPanel ... /\u003e\n    \u003c/RetroWindow\u003e\n  )\n}\nexport default App\n```\n\n### `src/index.css` (edit)\n```css\n/* keep */\n@import 'tailwindcss';\n:root {\n  /* replace the TaskFlow palette with a retro late-'90s MS Office palette per\n     ai-design/frontendstyle.jpg: teal desktop, silver/gray window chrome, navy title bar,\n     3D beveled borders (light top-left / dark bottom-right), yellow assistant balloon. */\n}\n/* remove the .board/.column/.card TaskFlow rules; add .retro-window, .titlebar,\n   .assistant, .balloon, beveled .btn, etc. */\n```\n\n## Test expectations\n- `src/App.test.tsx` (rewrite): a fresh visitor sees the \"Move\" title, a **Start** control, reminders paused (no active reminder bubble), and the default \"Office-safe\" loadout active; the mint UI exposes an axis picker; creating a custom exercise tagged with a newly minted axis-tagged tag makes it selectable/eligible; switching the active loadout changes the eligible set. Use role/label queries and `localStorage.clear()` in `beforeEach`.\n- Optionally colocate small component tests (e.g. `ReminderBubble.test.tsx` asserting name/`formatTarget`/instructions render and each of Done/Skip/Snooze/Shuffle fires `onAction`; `ProgressPanel.test.tsx` asserting the citation appears on the factual figure and the estimate line is labelled). Prefer covering flows through `App.test.tsx`.\n\n## Out of scope\nChanging logic modules, hooks, storage, or PWA infra (consume them as-is). Do not open or modify `ai-design/frontendstyle.jpg`.","status":"closed","priority":2,"issue_type":"task","assignee":"Wouter Molemaker","owner":"wouter.molemaker@simac-ids.com","created_at":"2026-07-22T22:52:46Z","created_by":"Wouter Molemaker","updated_at":"2026-07-23T08:05:25Z","started_at":"2026-07-23T07:55:18Z","closed_at":"2026-07-23T08:05:25Z","close_reason":"merged e5e066e6d0 into master via aip gate","labels":["awaiting-gate","epic:taskflowtest-6qf","guarded-changes","size:L"],"dependencies":[{"issue_id":"taskflowtest-af0","depends_on_id":"taskflowtest-cvo","type":"blocks","created_at":"2026-07-23T00:52:50Z","created_by":"Wouter Molemaker","metadata":"{}"},{"issue_id":"taskflowtest-af0","depends_on_id":"taskflowtest-m01","type":"blocks","created_at":"2026-07-23T00:52:50Z","created_by":"Wouter Molemaker","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"taskflowtest-m01","title":"PWA infra: manifest, service worker, assets, main.tsx registration","description":"## Goal\nAdd the installable-PWA infrastructure — web manifest, a plain-JS service worker with runtime caching and a `notificationclick` action pipeline, shipped static assets, `index.html` metadata, and service-worker registration in `main.tsx`.\n\n## Context\nMove is an installable PWA with no static precache list — the service worker caches at runtime. The SW is plain JS in `public/` (out of `src`) so no WebWorker types leak into `tsconfig`. This ticket is largely self-contained and touches disjoint files from the logic tickets; it only needs the app to render (the App shell already exists).\n\n## Acceptance criteria\n- `public/manifest.webmanifest`, `public/sw.js`, `public/icons/*` (at least 192px and 512px PNG), `public/exercises/placeholder.png` (a small real PNG), and `public/ding.wav` (a tiny real WAV) exist.\n- `index.html` has `\u003ctitle\u003eMove\u003c/title\u003e`, a `\u003clink rel=\"manifest\" href=\"/manifest.webmanifest\"\u003e`, and a `theme-color` meta.\n- `src/main.tsx` registers `/sw.js` (guarded by `'serviceWorker' in navigator`) and posts `client-ready` to the controller once mounted.\n- `sw.js` implements the caching + `notificationclick` behavior below.\n- `npm run lint`, `npm run build`, `npm test` pass (existing tests remain green).\n\n## File changes\nSketches are guidance; adapt to actual file state.\n\n### `public/manifest.webmanifest` (new)\n```json\n{\n  \"name\": \"Move\",\n  \"short_name\": \"Move\",\n  \"display\": \"standalone\",\n  \"start_url\": \"/\",\n  \"background_color\": \"#008080\",\n  \"theme_color\": \"#008080\",\n  \"icons\": [\n    { \"src\": \"/icons/icon-192.png\", \"sizes\": \"192x192\", \"type\": \"image/png\" },\n    { \"src\": \"/icons/icon-512.png\", \"sizes\": \"512x512\", \"type\": \"image/png\" }\n  ]\n}\n```\n\n### `public/sw.js` (new, plain JS)\n```js\nconst CACHE_NAME = 'move-v1'\nself.addEventListener('install', (e) =\u003e self.skipWaiting())\n// activate: delete every cache whose name !== CACHE_NAME, then clients.claim().\nself.addEventListener('activate', (e) =\u003e { /* caches.keys() -\u003e delete non-matching; clients.claim() */ })\n// fetch: GET only, same-origin.\n//   navigations / HTML: NETWORK-FIRST (on failure, cached response / cached '/').\n//   hashed build assets (/assets/...): CACHE-FIRST.\n//   other same-origin GET: STALE-WHILE-REVALIDATE.\nself.addEventListener('fetch', (e) =\u003e { /* ... */ })\n// notificationclick: event.notification.close(); then waitUntil:\n//   const data = event.notification.data (occurrenceId, exerciseId); action = event.action.\n//   const all = await clients.matchAll({ type:'window', includeUncontrolled:true });\n//   if a client exists: focus it and postMessage({ type:'reminder-action', action, exerciseId, occurrenceId }).\n//   else: enqueue the action (Cache or IndexedDB), then clients.openWindow('/'); deliver the queued\n//         action once the newly loaded client posts { type:'client-ready' }.\nself.addEventListener('notificationclick', (e) =\u003e { /* ... */ })\n// message: on { type:'client-ready' } from a client, flush any queued actions to that client.\nself.addEventListener('message', (e) =\u003e { /* ... */ })\n```\n\n### `public/icons/icon-192.png`, `public/icons/icon-512.png`, `public/exercises/placeholder.png`, `public/ding.wav` (new)\nShip tiny, license-clean binary placeholders (a solid-color/simple generated PNG per icon and the exercise placeholder; a \u003c1s generated WAV tone for the ding). These are swappable later.\n\n### `index.html` (edit)\n```html\n\u003c!-- before: \u003ctitle\u003e...\u003c/title\u003e in \u003chead\u003e, no manifest link --\u003e\n\u003c!-- after: --\u003e\n\u003ctitle\u003eMove\u003c/title\u003e\n\u003cmeta name=\"theme-color\" content=\"#008080\" /\u003e\n\u003clink rel=\"manifest\" href=\"/manifest.webmanifest\" /\u003e\n```\n\n### `src/main.tsx` (edit)\n```tsx\n// before: renders \u003cApp/\u003e only\n// after (additive): keep the render, then register the SW and announce readiness.\nif ('serviceWorker' in navigator) {\n  window.addEventListener('load', () =\u003e {\n    navigator.serviceWorker.register('/sw.js').catch(() =\u003e {})\n  })\n  navigator.serviceWorker.ready.then((reg) =\u003e {\n    reg.active?.postMessage({ type: 'client-ready' })\n  }).catch(() =\u003e {})\n}\n```\n\n## Test expectations\nNo new unit tests are required for `sw.js` (plain JS, out of the Vitest/jsdom scope) or the binary assets. Do NOT break the existing suite: `main.tsx` changes must be guarded so `npm test` and `npm run build` stay green. If you add any importable helper, colocate a small `*.test.ts`.\n\n## Out of scope\nThe engine hook that consumes SW messages (already implemented elsewhere), notification firing logic, and visual components. Do not modify `src/types.ts` or logic modules.","status":"closed","priority":2,"issue_type":"task","assignee":"Wouter Molemaker","owner":"wouter.molemaker@simac-ids.com","created_at":"2026-07-22T22:52:44Z","created_by":"Wouter Molemaker","updated_at":"2026-07-23T07:41:33Z","started_at":"2026-07-22T22:54:28Z","closed_at":"2026-07-23T07:41:33Z","close_reason":"merged df6c68de3f into master via aip gate","labels":["awaiting-gate","epic:taskflowtest-6qf","size:M"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"taskflowtest-cvo","title":"hooks: useMove (state owner) + useReminderEngine (leader-gated timer)","description":"## Goal\nImplement the two React hooks — `useMove` (owns `MoveState`, auto-persists on real change, merges cross-tab `storage` events) and `useReminderEngine` (ephemeral running/current, leader-gated timer with failover, snooze, empty-pool resume, SW-action draining) — with tests using fake timers.\n\n## Context\n`useMove` is the single state-owning hook (analogous to the old `useBoard`); `useReminderEngine` drives reminders only in the elected leader tab. Depends on foundation (`types`, `catalog`, `id`, `selection`) plus `storage` (t2), `progress` (t2), `loadouts` (t3), and `notifications`/`sound`/`engineChannel` (t4). Adapt imports to those modules' actual exports.\n\n## Acceptance criteria\n- `src/hooks/useMove.ts` exports `useMove()` returning `{ state, ...mutators }` (mutators below), auto-persisting only when the serialized state changed, and reconciling `storage` events via `mergeState` only when the serialized form differs.\n- `src/hooks/useReminderEngine.ts` exports `useReminderEngine(...)` returning `{ running, current, start, stop, handleAction }`.\n- All mutators stamp `updatedAt` on the entities they change.\n- `npm run lint`, `npm run build`, `npm test` pass.\n\n## File changes\nSketches are guidance; adapt to actual merged modules.\n\n### `src/hooks/useMove.ts` (new)\n```ts\nimport { useEffect, useRef, useState } from 'react'\nimport { loadState, mergeState, saveState, serialize, STORAGE_KEY } from '../storage'\nimport { importBundle as importBundleFn } from '../loadouts'\nimport { newId } from '../id'\nimport { normalizeTag } from '../catalog'\n\nexport function useMove() {\n  const [state, setState] = useState(loadState)\n  const lastSerialized = useRef(serialize(state))\n  // Persist only on real change.\n  useEffect(() =\u003e {\n    const s = serialize(state)\n    if (s === lastSerialized.current) return\n    lastSerialized.current = s\n    saveState(state)\n  }, [state])\n  // Cross-tab: on storage event for STORAGE_KEY, reload, mergeState, and setState ONLY if the\n  // serialized merged form differs from lastSerialized (prevents save\u003c-\u003emerge ping-pong).\n  useEffect(() =\u003e { /* addEventListener('storage', ...) + cleanup */ }, [])\n  // Mutators (each stamps updatedAt on touched entities and returns via setState):\n  //   setInterval(min), setSnooze(min), setContext(name), toggleEquipment(name),\n  //   setActiveLoadout(id), addExercise(draft), addTag(name, axis) [normalizeTag + dedupe by name],\n  //   saveLoadout(loadout), importBundle(raw, opts), recordAction(occurrenceId, exerciseId, action).\n  //   recordAction appends a HistoryEntry { id: newId(), occurrenceId, exerciseId, action, at }.\n  return { state, setInterval: setIntervalMin, setSnooze, setContext, toggleEquipment, setActiveLoadout, addExercise, addTag, saveLoadout, importBundle, recordAction }\n}\n```\nNote: name the interval mutator so it does not shadow the global `setInterval` (e.g. `setIntervalMinutes`).\n\n### `src/hooks/useReminderEngine.ts` (new)\n```ts\nimport { useEffect, useRef, useState } from 'react'\nimport { eligiblePool, selectNext } from '../selection'\nimport { trimHistory } from '../progress'\nimport { createEngineChannel } from '../engineChannel'\nimport { fireReminder, requestPermission } from '../notifications'\nimport { playDing } from '../sound'\nimport { MAX_DELAY_MS } from '../catalog'\nimport { newId } from '../id'\n\n// useReminderEngine({ state, recordAction, setState/trim hook, swRegistration }):\n//   ephemeral state: running (bool), current ({ exercise, occurrenceId } | null). NEVER persisted.\n//   start(): await requestPermission(); running = true; (re)start the leader loop.\n//   Leader loop (only while isLeader()): every settings.intervalMinutes a tick fires:\n//     pool = eligiblePool(state). If EMPTY: show the assistant \"no eligible exercises — adjust your\n//       loadout\", play NO ding/notification, but STILL reschedule the next tick (loadout switch resumes\n//       automatically). Else: ex = selectNext(pool, lastId); occurrenceId = newId(); set current;\n//       playDing(); fireReminder(swRegistration, ex, occurrenceId); leader-only trimHistory of state.\n//   setTimeout delays are clamped to MAX_DELAY_MS.\n//   handleAction({ action, occurrenceId, exerciseId }): recordAction(...) is allowed in ANY tab.\n//     STATE TRANSITIONS are LEADER-ONLY; a non-leader relays via engineChannel.relayAction and does\n//     nothing else. Clear/advance only affects `current` when occurrenceId === current.occurrenceId\n//     (a stale notification records its action but never dismisses an unrelated active reminder).\n//       done/skip =\u003e clear current. shuffle =\u003e re-pick (new occurrenceId), WITHOUT recording done.\n//       snooze =\u003e mint a NEW occurrenceId for the rescheduled fire; arm a snooze timer KEYED by the\n//         triggering occurrenceId (arming replaces any existing timer for that key); broadcastSnooze\n//         so a new leader adopts it.\n//   engineChannel handlers: onAction =\u003e apply as leader; onAdoptSnooze =\u003e arm keyed snooze timer.\n//   On mount: drain any pending SW-queued actions (listen for navigator.serviceWorker 'message'\n//     {type:'reminder-action'} and process the queue), then post 'client-ready' is done in main.tsx.\n//   Cleanup: clear timers, engineChannel.close().\nexport function useReminderEngine(/* ... */) { /* ... */ return { running, current, start, stop, handleAction } }\n```\n\n## Test expectations\n- `src/hooks/useMove.test.ts` (or `.tsx`): with `@testing-library/react` `renderHook`, an incoming `storage` event carrying an equal-but-reordered serialized state does NOT trigger a re-save (spy on `saveState`/`localStorage.setItem`); a mutator stamps `updatedAt` and persists once.\n- `src/hooks/useReminderEngine.test.ts` (fake timers; mock `notifications`, `sound`, `engineChannel`): an interval fire calls `fireReminder` and `playDing`; a snooze re-fire uses a NEW `occurrenceId`; a huge `intervalMinutes` clamps the `setTimeout` delay to `MAX_DELAY_MS`; `handleAction` with `occurrenceId !== current.occurrenceId` does NOT clear the active reminder but still records; shuffle advances without recording `done`; a non-leader relays and applies no transition; leader resign/timeout lets a peer continue; an empty pool still reschedules the next tick; a pending SW-queued action is drained on mount.\n\n## Out of scope\nThe service worker file and `main.tsx` (another ticket), and all visual components. Consume the already-merged modules; do not modify them beyond importing.","status":"closed","priority":2,"issue_type":"task","assignee":"Wouter Molemaker","owner":"wouter.molemaker@simac-ids.com","created_at":"2026-07-22T22:52:43Z","created_by":"Wouter Molemaker","updated_at":"2026-07-23T07:55:15Z","started_at":"2026-07-23T07:43:07Z","closed_at":"2026-07-23T07:55:15Z","close_reason":"merged fd2fc979e2 into master via aip gate","labels":["awaiting-gate","epic:taskflowtest-6qf","guarded-changes","size:L"],"dependencies":[{"issue_id":"taskflowtest-cvo","depends_on_id":"taskflowtest-e7m","type":"blocks","created_at":"2026-07-23T00:52:49Z","created_by":"Wouter Molemaker","metadata":"{}"},{"issue_id":"taskflowtest-cvo","depends_on_id":"taskflowtest-55d","type":"blocks","created_at":"2026-07-23T00:52:48Z","created_by":"Wouter Molemaker","metadata":"{}"},{"issue_id":"taskflowtest-cvo","depends_on_id":"taskflowtest-5lf","type":"blocks","created_at":"2026-07-23T00:52:49Z","created_by":"Wouter Molemaker","metadata":"{}"},{"issue_id":"taskflowtest-cvo","depends_on_id":"taskflowtest-yxn","type":"blocks","created_at":"2026-07-23T00:52:48Z","created_by":"Wouter Molemaker","metadata":"{}"}],"dependency_count":4,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"taskflowtest-5lf","title":"notifications.ts + sound.ts + engineChannel.ts: OS notifications, ding, cross-tab leader/relay","description":"## Goal\nImplement the three browser-integration modules — notification firing (rich vs fallback), the retro ding, and the `BroadcastChannel` leader-election/relay layer — each guarded for jsdom/absent APIs and unit-tested.\n\n## Context\nMove fires exercise reminders via the Notifications API and coordinates a single active timer across tabs via `BroadcastChannel('move.engine')`. All three modules must feature-detect and degrade gracefully (jsdom has neither `Notification`, `Audio`, nor a useful `BroadcastChannel`). Depends on `src/types.ts`, `src/catalog.ts` (`formatTarget`), and `src/id.ts` (`newId`).\n\n## Acceptance criteria\n- `src/notifications.ts` exports `requestPermission`, `maxInlineActions`, `orderedActions`, `fireReminder`.\n- `src/sound.ts` exports `playDing`.\n- `src/engineChannel.ts` exports `createEngineChannel` returning the API below, with a no-op shim when `BroadcastChannel` is unavailable.\n- Every DOM/global access is guarded so the modules import cleanly under jsdom.\n- `npm run lint`, `npm run build`, `npm test` pass.\n\n## File changes\nSketches are guidance; adapt to actual foundation state.\n\n### `src/notifications.ts` (new)\n```ts\nimport type { Exercise, ReminderAction } from './types'\nimport { formatTarget } from './catalog'\n\n// requestPermission: if typeof Notification === 'undefined' return 'denied';\n//   if Notification.permission === 'default' return await Notification.requestPermission();\n//   else return Notification.permission.\nexport async function requestPermission(): Promise\u003cNotificationPermission | 'denied'\u003e { /* ... */ }\n\n// maxInlineActions: (typeof Notification !== 'undefined' \u0026\u0026 'maxActions' in Notification) ? Notification.maxActions : 0\nexport function maxInlineActions(): number { /* ... */ }\n\n// orderedActions(max): priority order ['done','snooze','skip','shuffle']; return the first min(4, max).\nexport function orderedActions(max: number): ReminderAction[] { /* ... */ }\n\n// fireReminder(reg, exercise, occurrenceId):\n//   body = `${formatTarget(exercise.target)} — ${exercise.instructions}` (title = exercise.name).\n//   Rich path (reg \u0026\u0026 maxInlineActions() \u003e 0): reg.showNotification(exercise.name, {\n//     body, icon: exercise.image, image: exercise.image,\n//     actions: orderedActions(maxInlineActions()).map(a =\u003e ({ action: a, title: \u003cLabel\u003e })),\n//     data: { occurrenceId, exerciseId: exercise.id } }).\n//   Fallback (no reg / maxActions === 0): if Notification permission granted, new Notification(exercise.name, { body }).\n//   Guard everything; never throw.\nexport async function fireReminder(reg: ServiceWorkerRegistration | null, exercise: Exercise, occurrenceId: string): Promise\u003cvoid\u003e { /* ... */ }\n```\n\n### `src/sound.ts` (new)\n```ts\n// playDing: if typeof Audio === 'undefined' return; try { const a = new Audio('/ding.wav'); void a.play().catch(() =\u003e {}) } catch {}\nexport function playDing(): void { /* ... */ }\n```\n\n### `src/engineChannel.ts` (new)\n```ts\nimport { newId } from './id'\n\nexport type ReminderMsg = { type: 'reminder'; exerciseId: string; occurrenceId: string }\nexport type ActionMsg = { type: 'action'; action: string; occurrenceId: string; exerciseId: string; actionEventId: string }\nexport type SnoozeMsg = { type: 'snooze'; key: string; exerciseId: string; fireAt: number }\n\nexport type EngineHandlers = {\n  onBecomeLeader?: () =\u003e void\n  onLoseLeader?: () =\u003e void\n  onAction?: (msg: ActionMsg) =\u003e void       // leader applies relayed actions\n  onAdoptSnooze?: (msg: SnoozeMsg) =\u003e void   // new leader adopts a still-future snooze\n}\n\n// createEngineChannel(handlers):\n//   - If typeof BroadcastChannel === 'undefined', return a no-op shim whose isLeader() === true\n//     (each tab runs its own engine; duplicate notifications possible — documented).\n//   - Otherwise open BroadcastChannel('move.engine'). Assign a stable tabPriority = newId().\n//     Election: broadcast candidacy; LOWEST tabPriority wins (deterministic tiebreak).\n//     Leader emits a heartbeat every 2s. A non-leader that sees no heartbeat for 6s re-runs election.\n//     On pagehide / visibilitychange(hidden) / beforeunload the leader broadcasts an explicit 'resign'.\n//     relayAction: non-leader sends ActionMsg (with actionEventId = newId()) to the leader and RETRIES\n//       until acked or the effect is observed; the leader keeps a short-lived processed set of\n//       actionEventIds, applies each once via onAction, and ACKs. broadcastSnooze sends SnoozeMsg;\n//       a newly elected leader adopts any still-future snooze via onAdoptSnooze (keyed by SnoozeMsg.key).\nexport function createEngineChannel(handlers: EngineHandlers): {\n  isLeader: () =\u003e boolean\n  relayAction: (a: Omit\u003cActionMsg, 'type' | 'actionEventId'\u003e) =\u003e void\n  broadcastSnooze: (s: Omit\u003cSnoozeMsg, 'type'\u003e) =\u003e void\n  close: () =\u003e void\n} { /* ... */ }\n```\nNote: timers (heartbeat/failover) must use real `setInterval`/`setTimeout` so tests can drive them with fake timers.\n\n## Test expectations\n- `src/notifications.test.ts`: `orderedActions(2)` returns `['done','snooze']` (priority truncation); `orderedActions(0)` returns `[]`; with a mocked `registration.showNotification`, `fireReminder` sets title = exercise name, body includes `formatTarget(target)`, and `data` carries `{ occurrenceId, exerciseId }`; falls back to plain `Notification` when `maxInlineActions()` is 0. Stub `Notification`/`ServiceWorkerRegistration` as needed.\n- `src/sound.test.ts`: `playDing` does not throw when `Audio` is undefined and, with a mocked `Audio`, constructs `'/ding.wav'` and swallows a rejected `play()`.\n- `src/engineChannel.test.ts` (fake timers + a mock `BroadcastChannel` that fans messages to peer instances): lowest-`tabPriority` wins election; a heartbeat timeout AND an explicit resign each trigger re-election; a relayed action with an `actionEventId` is applied once and NOT double-applied across a simulated mid-processing leader death + retry (single effect); an armed-snooze broadcast is adopted (`onAdoptSnooze`) by a newly elected leader; the no-`BroadcastChannel` shim reports `isLeader() === true`.\n\n## Out of scope\nThe engine hook that consumes this (`useReminderEngine`), the service worker file, and UI. Do not modify foundation modules beyond importing.","status":"closed","priority":2,"issue_type":"task","assignee":"Wouter Molemaker","owner":"wouter.molemaker@simac-ids.com","created_at":"2026-07-22T22:52:42Z","created_by":"Wouter Molemaker","updated_at":"2026-07-22T23:23:45Z","started_at":"2026-07-22T23:00:17Z","closed_at":"2026-07-22T23:23:45Z","close_reason":"merged 8cc36cddb7 into master via aip gate","labels":["awaiting-gate","epic:taskflowtest-6qf","guarded-changes","size:L"],"dependencies":[{"issue_id":"taskflowtest-5lf","depends_on_id":"taskflowtest-yxn","type":"blocks","created_at":"2026-07-23T00:52:47Z","created_by":"Wouter Molemaker","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"taskflowtest-e7m","title":"loadouts.ts: export/import self-contained loadout bundles","description":"## Goal\nImplement `exportBundle` and `importBundle` for shareable loadout bundles, with size/safety/referential-completeness guards, id-collision handling, normalized tag merge, and axis-conflict keep-or-rename — unit-tested.\n\n## Context\nA user exports a loadout as a self-contained JSON bundle `{ version: 1, loadout, exercises, tags }` and imports it elsewhere. Bundles are UNTRUSTED. This is pure TS depending on `src/types.ts`, `src/catalog.ts`, and `src/selection.ts` (uses `matchesLoadout`).\n\n## Acceptance criteria\n- `src/loadouts.ts` exports `exportBundle(state, loadoutId): LoadoutBundle` and `importBundle(state, raw, opts?): ImportResult`.\n- All guards below are enforced up front with clear thrown `Error`s.\n- `npm run lint`, `npm run build`, `npm test` pass.\n\n## File changes\nSketches are guidance; adapt to the actual state of already-merged foundation modules.\n\n### `src/loadouts.ts` (new)\n```ts\nimport type { Exercise, ImportResult, Loadout, LoadoutBundle, MoveState, TagDef } from './types'\nimport { MAX_BUNDLE_BYTES, MAX_IMAGE_BYTES, isSafeImage, normalizeTag } from './catalog'\nimport { matchesLoadout } from './selection'\nimport { newId } from './id'\n\n// exportBundle(state, loadoutId):\n//   loadout = state.loadouts.find(id === loadoutId) (throw if missing)\n//   exercises = state.exercises.filter(ex =\u003e ex.custom \u0026\u0026 matchesLoadout(ex, loadout))  // equipment/context-agnostic\n//   referenced tag names = normalized union of every exercise.tags of those exercises\n//     AND loadout.include/requireAll/exclude\n//   tags = state.tags.filter(t =\u003e referenced.has(normalizeTag(t.name)))\n//   return { version: 1, loadout, exercises, tags }\nexport function exportBundle(state: MoveState, loadoutId: string): LoadoutBundle { /* ... */ }\n\n// opts: { axisConflicts?: Record\u003cstring /*normalized name*/, 'keep' | 'rename'\u003e } (default per-name 'keep')\n// importBundle(state, raw, opts?):\n//   1. Parse: accept an object or a JSON string; measure byte size via\n//      new TextEncoder().encode(JSON.stringify(bundle)).length \u003c= MAX_BUNDLE_BYTES else throw.\n//   2. Reject bundle.version !== 1.\n//   3. Guard shape of loadout/exercises/tags; for each exercise: isSafeImage(image) else throw,\n//      and image byte size (TextEncoder length) \u003c= MAX_IMAGE_BYTES else throw.\n//   4. Referential completeness: every tag name referenced by an imported exercise's tags OR by\n//      the loadout's rule arrays must resolve (normalized) to a TagDef in bundle.tags UNION state.tags;\n//      a missing referenced TagDef is a HARD error (throw) — never default to 'other'.\n//   5. Merge tags by normalized name into state.tags. Same name + SAME axis =\u003e keep greater updatedAt.\n//      Same name + DIFFERENT axis =\u003e consult opts.axisConflicts[name]:\n//        'keep'   =\u003e keep local axis, push warning { name, localAxis, bundleAxis }.\n//        'rename' =\u003e mint a fresh normalized tag name carrying the BUNDLE axis, and rewrite that name\n//                    in the incoming exercises' tags AND the loadout's rule arrays.\n//   6. Merge exercises by id: id unused locally =\u003e keep as-is; id collides with a DIFFERENT local\n//      exercise =\u003e assign a fresh newId() (no clobber; no reference remap needed — loadouts reference\n//      tag names, not exercise ids, and bundles carry no history); identical to local =\u003e skip.\n//   7. Append the loadout (fresh newId() if its id collides).\n//   Return { state: mergedState, warnings }.\nexport function importBundle(state: MoveState, raw: unknown, opts?: { axisConflicts?: Record\u003cstring, 'keep' | 'rename'\u003e }): ImportResult { /* ... */ }\n```\n\n## Test expectations\n- `src/loadouts.test.ts`:\n  - `exportBundle` embeds all filter-passing CUSTOM exercises and exactly the `TagDef`s referenced by those exercises' tags and by the loadout's rule arrays (no extras).\n  - `importBundle` rejects `version !== 1`; rejects an oversize bundle (\u003e `MAX_BUNDLE_BYTES`) and an oversize image (\u003e `MAX_IMAGE_BYTES`); rejects unsafe images `data:text/html,...`, `javascript:...`, `data:image/svg+xml,...`, `data:image/SVG,...`, `data:image/svg,...`; rejects a bundle referencing a tag name with no resolvable `TagDef`.\n  - id collision with a different local exercise mints a fresh id (original local exercise untouched); an identical exercise is skipped (no duplicate).\n  - axis conflict with `'keep'` keeps the local axis and records a warning `{ name, localAxis, bundleAxis }`; `'rename'` mints a fresh tag with the bundle axis and rewrites both the incoming exercises' tags and the loadout's rule arrays.\n  - normalized merge collapses `Bands` and `bands` to one tag.\n  - round-trip (`exportBundle` then `importBundle` into a fresh default state) reproduces the loadout's eligibility (same eligible exercise set).\n\n## Out of scope\nUI wiring of the conflict picker, storage/mergeState, engine, hooks. Do not modify `types.ts`/`catalog.ts`/`selection.ts` beyond importing.","status":"closed","priority":2,"issue_type":"task","assignee":"Wouter Molemaker","owner":"wouter.molemaker@simac-ids.com","created_at":"2026-07-22T22:52:41Z","created_by":"Wouter Molemaker","updated_at":"2026-07-23T07:37:59Z","started_at":"2026-07-22T23:00:20Z","closed_at":"2026-07-23T07:37:59Z","close_reason":"merged 3f610db84d into master via aip gate","labels":["awaiting-gate","epic:taskflowtest-6qf","guarded-changes","needs-human","size:M"],"dependencies":[{"issue_id":"taskflowtest-e7m","depends_on_id":"taskflowtest-yxn","type":"blocks","created_at":"2026-07-23T00:52:47Z","created_by":"Wouter Molemaker","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"taskflowtest-55d","title":"storage.ts + progress.ts: persistence, cross-tab merge, history dedupe/stats","description":"## Goal\nImplement Move's localStorage persistence with a commutative/idempotent cross-tab merge, plus history dedupe, derived stats, and leader-only history trimming — all unit-tested.\n\n## Context\nMove keeps all state in one localStorage key as a `MoveState`. Multiple tabs write concurrently, so merges must converge deterministically regardless of order (no last-write-wins ping-pong). History is deduped canonically at read/merge time; old entries fold into a `rollup`. This ticket is pure TS depending on the foundation module (`src/catalog.ts`, `src/types.ts`).\n\n## Acceptance criteria\n- `src/storage.ts` exports `STORAGE_KEY = 'move.state'`, `serialize`, `loadState`, `mergeState`, `saveState`.\n- `src/progress.ts` exports `dedupeHistory`, `deriveStats`, `trimHistory`, `SITTING_BREAKS_SOURCE`, `estActiveMinutes`.\n- `mergeState` is commutative and idempotent: `serialize(mergeState(a,b)) === serialize(mergeState(b,a))` and `serialize(mergeState(a,a)) === serialize(a)`.\n- `npm run lint`, `npm run build`, `npm test` pass.\n\n## File changes\nSketches are guidance; adapt to actual file state (foundation symbols already exist from a merged earlier ticket).\n\n### `src/storage.ts` (rewrite — file currently holds the TaskFlow board loader)\n```ts\nimport type { MoveState, /* ... */ } from './types'\nimport { createDefaultState, HISTORY_CAP /* if needed */ } from './catalog'\n\nexport const STORAGE_KEY = 'move.state'\n\n// serialize: STABLE canonical JSON. Recursively sort object keys; sort arrays by a\n// canonical key so equal content always yields byte-identical output:\n//   exercises by id, loadouts by id, tags by normalized name, history by entry id,\n//   settings.ownedEquipment and every loadout rule array sorted ascending.\nexport function serialize(state: MoveState): string { /* ... */ }\n\n// loadState: read STORAGE_KEY; JSON.parse in try/catch; validate every field with\n// per-field type guards; drop invalid entries; fill missing sections from\n// createDefaultState(); then re-merge built-ins so built-in exercises/tags are always\n// present (e.g. mergeState(createDefaultState(), loaded)). Return a valid MoveState;\n// return createDefaultState() on empty or unparseable storage.\nexport function loadState(): MoveState { /* ... */ }\n\n// mergeState(a,b): commutative, idempotent fixpoint.\n//   exercises \u0026 loadouts: union by id; per id pick greater updatedAt; tie =\u003e the entity\n//     with the lexicographically smaller canonical serialization.\n//   tags: union by normalized name; greater updatedAt; tie =\u003e smaller canonical serialization.\n//   settings: single versioned object; pick greater updatedAt; tie =\u003e smaller serialization.\n//   history: union by entry id; dedupe; then DROP any entry whose at \u003c= the merged\n//     rollup.trimmedThroughAt.\n//   rollup: take the side with the greater trimmedThroughAt; if equal, field-wise\n//     max(done), max(ignored), union(doneDayKeys) (kept commutative/deterministic).\n//   Result arrays canonically sorted (reuse serialize's ordering).\nexport function mergeState(a: MoveState, b: MoveState): MoveState { /* ... */ }\n\n// saveState: const s = serialize(state); if s === lastWritten (module-level cache) return;\n//   try { localStorage.setItem(STORAGE_KEY, s); lastWritten = s } catch { console.warn(...) }\nexport function saveState(state: MoveState): void { /* ... */ }\n```\n\n### `src/progress.ts` (new)\n```ts\nimport type { HistoryEntry, MoveState, Rollup } from './types'\nimport { DEDUPE_WINDOW_MS, HISTORY_CAP, localDayKey, pruneDoneDayKeys } from './catalog'\n\nexport const SITTING_BREAKS_SOURCE = 'Diaz et al., Annals of Internal Medicine, 2017'\n\n// dedupeHistory: (1) collapse entries sharing (occurrenceId, action) to one, keeping the\n//   earliest `at`; (2) secondary collapse of entries sharing (exerciseId, action) whose\n//   `at` values fall within DEDUPE_WINDOW_MS of each other, keeping earliest. Deterministic.\nexport function dedupeHistory(entries: HistoryEntry[]): HistoryEntry[] { /* ... */ }\n\n// deriveStats(rollup, history, now?): dedupe entries with at \u003e rollup.trimmedThroughAt.\n//   done = rollup.done + count of distinct collapsed 'done'\n//   ignored = rollup.ignored + count of distinct collapsed 'skip'\n//   snooze/shuffle count toward neither.\n//   streak: from the set of doneDayKeys UNION localDayKey(at) of retained done entries,\n//     count consecutive local calendar days ending at localDayKey(now) (0 if today absent).\n//   Return { done, ignored, streak, sittingBreaks: done, estActiveMinutes: done * 2, source: SITTING_BREAKS_SOURCE }.\nexport function deriveStats(rollup: Rollup, history: HistoryEntry[], now?: number): { done: number; ignored: number; streak: number; sittingBreaks: number; estActiveMinutes: number; source: string }\n\n// estActiveMinutes: playful estimate helper (done * 2), NOT citation-attributed. May be inlined in deriveStats.\nexport function estActiveMinutes(doneCount: number): number { return doneCount * 2 }\n\n// trimHistory(state): LEADER-ONLY. If history.length \u003c= HISTORY_CAP return state unchanged.\n//   Otherwise sort by at ascending, take the oldest (length - HISTORY_CAP) as `dropped`,\n//   dedupe dropped, fold their totals into rollup: done += distinct 'done', ignored += distinct\n//   'skip', doneDayKeys := pruneDoneDayKeys([...rollup.doneDayKeys, ...localDayKey(at) of dropped 'done']),\n//   trimmedThroughAt := max at among dropped. Keep the most-recent HISTORY_CAP entries.\nexport function trimHistory(state: MoveState): MoveState { /* ... */ }\n```\n\n## Test expectations\n- `src/storage.test.ts`: `loadState` returns defaults on empty and on corrupt/unparseable storage; drops invalid entries via guards but keeps valid ones; `mergeState` is commutative (`serialize(mergeState(a,b)) === serialize(mergeState(b,a))`) and idempotent; divergent same-id exercises/loadouts and divergent settings resolve to the greater `updatedAt` with lexicographic tiebreak (no ping-pong: merging repeatedly is stable); history entries with `at \u003c= trimmedThroughAt` are dropped on merge; a union preserves a creation present only in a stale tab; rollup keeps the greater `trimmedThroughAt`; `saveState` writes once then skips an identical re-save (assert `localStorage.setItem` call count via spy).\n- `src/progress.test.ts`: `dedupeHistory` collapses same `(occurrenceId, action)` and collapses split-brain distinct-occurrence same-`(exerciseId, action)` within `DEDUPE_WINDOW_MS` (and does NOT collapse outside the window); `deriveStats` done/ignored exclude snooze/shuffle; streak is DST-stable (use fixed `at` timestamps + `now`); `trimHistory` folds dropped totals into `rollup` and `pruneDoneDayKeys` bounds `doneDayKeys` to the current-streak span; the returned stats expose `SITTING_BREAKS_SOURCE` on the factual figure and an unattributed `estActiveMinutes`.\n\n## Out of scope\nUI, hooks, engine, notifications, loadout import/export. Do not change `src/types.ts` or `src/catalog.ts` beyond importing from them.","status":"closed","priority":2,"issue_type":"task","assignee":"Wouter Molemaker","owner":"wouter.molemaker@simac-ids.com","created_at":"2026-07-22T22:52:39Z","created_by":"Wouter Molemaker","updated_at":"2026-07-23T07:36:13Z","started_at":"2026-07-22T23:00:23Z","closed_at":"2026-07-23T07:36:13Z","close_reason":"merged ea330b8da9 into master via aip gate","labels":["awaiting-gate","epic:taskflowtest-6qf","guarded-changes","needs-human","size:L"],"dependencies":[{"issue_id":"taskflowtest-55d","depends_on_id":"taskflowtest-yxn","type":"blocks","created_at":"2026-07-23T00:52:47Z","created_by":"Wouter Molemaker","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"taskflowtest-yxn","title":"Foundation: teardown, types, id, catalog, selection","description":"## Goal\nRemove the TaskFlow Kanban domain and establish the Move foundation — shared types, an id generator, the catalog/constants/helpers module, and eligibility/selection logic — all unit-tested and building green.\n\n## Context\nThis is the first ticket of a rewrite: TaskFlow becomes \"Move\", a no-backend client-side app whose state lives in localStorage. This ticket ships only the pure-TS foundation every later ticket imports. Do NOT build storage, progress, loadouts, notifications, the engine, hooks, components, or PWA infra here.\n\n## Acceptance criteria\n- These files are DELETED: `src/board.ts`, `src/board.test.ts`, `src/hooks/useBoard.ts`, `src/components/Board.tsx`, `src/components/Column.tsx`, `src/components/TaskCard.tsx`.\n- `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` are removed from `package.json` dependencies and no `@dnd-kit` import remains anywhere in `src`.\n- `src/types.ts` exports exactly the Move type set below.\n- `src/id.ts`, `src/catalog.ts`, `src/selection.ts` exist and export the listed symbols with the specified behavior.\n- `src/App.tsx` and `src/App.test.tsx` are reduced to a minimal shell that renders an `\u003ch1\u003eMove\u003c/h1\u003e` and imports none of the deleted files.\n- `npm run lint`, `npm run build`, and `npm test` all pass.\n\n## File changes\nSketches are guidance only — parallel merges may shift surrounding code; adapt to each file's actual state.\n\n### `package.json` (edit)\n```\n// before\n  \"dependencies\": {\n    \"@dnd-kit/core\": \"^6.3.1\",\n    \"@dnd-kit/sortable\": \"^10.0.0\",\n    \"@dnd-kit/utilities\": \"^3.2.2\",\n    \"@tailwindcss/vite\": \"^4.3.3\",\n    \"react\": \"^19.2.7\",\n    ...\n// after — the three @dnd-kit lines removed, rest untouched\n  \"dependencies\": {\n    \"@tailwindcss/vite\": \"^4.3.3\",\n    \"react\": \"^19.2.7\",\n    ...\n```\n\n### `src/types.ts` (rewrite)\n```ts\n// before: Priority/ColumnId/Task/Board\n// after (exact):\nexport type TagAxis = 'equipment' | 'context' | 'type' | 'intensity' | 'duration' | 'other'\nexport type TagDef = { name: string; axis: TagAxis; updatedAt: number }\nexport type ExerciseTarget = { kind: 'reps'; reps: number } | { kind: 'time'; seconds: number }\nexport type Exercise = { id: string; name: string; instructions: string; target: ExerciseTarget; image: string; tags: string[]; custom: boolean; updatedAt: number }\nexport type Loadout = { id: string; name: string; include: string[]; requireAll: string[]; exclude: string[]; updatedAt: number }\nexport type ReminderAction = 'done' | 'skip' | 'snooze' | 'shuffle'\nexport type HistoryEntry = { id: string; occurrenceId: string; exerciseId: string; action: ReminderAction; at: number }\nexport type Rollup = { done: number; ignored: number; doneDayKeys: string[]; trimmedThroughAt: number }\nexport type Settings = { intervalMinutes: number; snoozeMinutes: number; activeContext: string; ownedEquipment: string[]; activeLoadoutId: string; updatedAt: number }\nexport type MoveState = { settings: Settings; exercises: Exercise[]; loadouts: Loadout[]; history: HistoryEntry[]; rollup: Rollup; tags: TagDef[] }\nexport type LoadoutBundle = { version: 1; loadout: Loadout; exercises: Exercise[]; tags: TagDef[] }\nexport type ImportResult = { state: MoveState; warnings: { name: string; localAxis: TagAxis; bundleAxis: TagAxis }[] }\n```\n\n### `src/id.ts` (new)\n```ts\n// newId(): prefer crypto.randomUUID(); else build a v4 UUID from crypto.getRandomValues;\n// else fall back to `${Date.now().toString(36)}-${(counter++).toString(36)}`.\n// Must never throw in a non-secure context / old webview (guard every access).\nexport function newId(): string { /* ... */ }\n```\n\n### `src/catalog.ts` (new)\nExport:\n- Constants: `MAX_DELAY_MS = 2 ** 31 - 1`, `HISTORY_CAP = 5000`, `DEDUPE_WINDOW_MS = 10_000`, `MAX_IMAGE_BYTES = 512 * 1024`, `MAX_BUNDLE_BYTES = 2 * 1024 * 1024`.\n- `PLACEHOLDER_IMAGE: string` — a small inline `data:image/png;base64,...` PNG data URL used as the default exercise image.\n- `normalizeTag(name: string): string` = `name.trim().toLowerCase()`.\n- `axisOf(tags: TagDef[], name: string): TagAxis` — find the TagDef whose normalized name equals `normalizeTag(name)`; return its axis, else `'other'`.\n- `formatTarget(target: ExerciseTarget): string` — reps: `` `${target.reps} reps` ``; time: `` `${target.seconds}s` ``.\n- `localDayKey(ms: number): string` — build `` `${y}-${mm}-${dd}` `` from a `new Date(ms)` using LOCAL date parts (zero-padded). Never UTC/ms-diff (DST-safe streaks).\n- `isSafeImage(s: string): boolean` — true iff `s === PLACEHOLDER_IMAGE`, OR (case-insensitively) `s` starts with `data:`, has a media type (the substring between `data:` and the first `;` or `,`) whose lowercased value is in the raster allowlist `['image/png','image/jpeg','image/gif','image/webp','image/avif','image/bmp']`. This intentionally blocks every SVG variant (`image/svg+xml`, `image/svg`, any casing) and non-image `data:` payloads. Anything not matching → false.\n- `resolveActiveLoadout(state: MoveState): Loadout` — return the loadout whose id equals `state.settings.activeLoadoutId`; if that id is dangling, return the default loadout (the one named `Office-safe`) or, failing that, `state.loadouts[0]`.\n- `pruneDoneDayKeys(keys: string[]): string[]` — dedupe, sort descending; starting at the newest key, keep each key only while it is exactly one calendar day earlier than the previously kept key; stop at the first gap (keys before a gap can never extend today's streak). Return the kept run.\n- `createDefaultState(): MoveState` — seed: `settings = { intervalMinutes: 30, snoozeMinutes: 5, activeContext: 'office', ownedEquipment: [], activeLoadoutId: \u003cdefault loadout id\u003e, updatedAt: 0 }`; `exercises = BUILTIN_EXERCISES`; `loadouts = [DEFAULT_LOADOUT]`; `history = []`; `rollup = { done: 0, ignored: 0, doneDayKeys: [], trimmedThroughAt: 0 }`; `tags = SEEDED_TAGS`.\n- Built-ins must use STABLE literal ids and `updatedAt: 0` (so a user edit with a higher `updatedAt` wins on later merge, and re-seeding on load is idempotent). Ship ~6–8 curated office-safe exercises (e.g. neck rolls, shoulder shrugs, seated marches, calf raises, wrist stretches) each with `custom: false`, `image: PLACEHOLDER_IMAGE`, a structured `target`, and normalized `tags`. `DEFAULT_LOADOUT = { id: 'loadout-office-safe', name: 'Office-safe', include: [], requireAll: [], exclude: [], updatedAt: 0 }`.\n- `SEEDED_TAGS` must include the context tags `office`, `home`, `outdoors` (axis `'context'`) plus every tag referenced by the built-in exercises, each normalized with an explicit axis and `updatedAt: 0`. Built-in exercises must carry NO equipment-axis tags (so they are eligible with empty `ownedEquipment`).\n\n### `src/selection.ts` (new)\n```ts\n// All tag comparisons normalize both sides via normalizeTag.\n// matchesLoadout(ex, loadout): rule-only (no context/equipment):\n//   include:   loadout.include empty ⇒ pass, else ex shares \u003e=1 include tag\n//   requireAll: every requireAll tag is present on ex\n//   exclude:   ex carries none of the exclude tags\nexport function matchesLoadout(ex: Exercise, loadout: Loadout): boolean\n// isEligible(ex, state, loadout, tags): matchesLoadout AND context AND equipment.\n//   context: ex carries NO context-axis tag (agnostic) OR carries state.settings.activeContext;\n//            a dangling/empty activeContext (not a known context tag) ⇒ no context constraint.\n//   equipment: for every equipment-axis tag on ex, that tag is in state.settings.ownedEquipment.\nexport function isEligible(ex: Exercise, state: MoveState, loadout: Loadout): boolean\n// eligiblePool(state): loadout = resolveActiveLoadout(state); return exercises passing isEligible.\nexport function eligiblePool(state: MoveState): Exercise[]\n// selectNext(pool, lastId?): null if empty; if pool has \u003e1 item, exclude lastId then pick random; else pick the single item.\nexport function selectNext(pool: Exercise[], lastId?: string): Exercise | null\n```\n\n### `src/App.tsx` (rewrite to minimal shell)\n```tsx\n// before: imports Board and renders TaskFlow header + \u003cBoard/\u003e\n// after:\nfunction App() {\n  return (\n    \u003cdiv className=\"app\"\u003e\n      \u003ch1\u003eMove\u003c/h1\u003e\n    \u003c/div\u003e\n  )\n}\nexport default App\n```\n(Note: a later ticket rewrites App.tsx fully; this shell only keeps the build green after the Kanban components are deleted.)\n\n### `src/App.test.tsx` (rewrite to minimal)\n```tsx\nimport { beforeEach, describe, expect, it } from 'vitest'\nimport { render, screen } from '@testing-library/react'\nimport App from './App'\nbeforeEach(() =\u003e localStorage.clear())\ndescribe('Move app', () =\u003e {\n  it('renders the Move title', () =\u003e {\n    render(\u003cApp /\u003e)\n    expect(screen.getByRole('heading', { name: 'Move' })).toBeInTheDocument()\n  })\n})\n```\n\n## Test expectations\n- `src/id.test.ts`: `newId` returns unique strings when `crypto.randomUUID` exists; when only `crypto.getRandomValues` exists (stub `randomUUID` undefined); and when neither exists (no throw, still unique across calls).\n- `src/catalog.test.ts`: `normalizeTag` trims + lowercases; `isSafeImage` accepts `PLACEHOLDER_IMAGE` and `data:image/png;base64,...`, rejects `data:text/html,...`, `javascript:alert(1)`, `data:image/svg+xml,...`, `data:image/SVG,...`, `data:image/svg,...`; `axisOf` resolves seeded tags and defaults unknown to `'other'`; `formatTarget` renders reps and time forms; `localDayKey` is stable for same local day; `resolveActiveLoadout` returns the active loadout and falls back to Office-safe when the id is dangling; `pruneDoneDayKeys` keeps the contiguous most-recent run and drops keys before a gap; `createDefaultState` has 30/5 interval/snooze, empty ownedEquipment, active loadout resolvable, and built-ins carry no equipment-axis tags.\n- `src/selection.test.ts`: include-empty passes; require-all enforced; exclude removes; equipment-axis tag not owned excludes; context non-match excluded but context-agnostic passes; dangling `activeLoadoutId` falls back to default (non-empty pool, no crash); dangling `activeContext` ⇒ no constraint; `selectNext` avoids `lastId` and returns `null` on empty pool.\n\n## Out of scope\nstorage/mergeState, progress, loadouts import/export, notifications, sound, engineChannel, hooks, all real UI components, PWA infra. Only App.tsx as a throwaway shell.","status":"closed","priority":2,"issue_type":"task","assignee":"Wouter Molemaker","owner":"wouter.molemaker@simac-ids.com","created_at":"2026-07-22T22:52:38Z","created_by":"Wouter Molemaker","updated_at":"2026-07-22T23:00:16Z","started_at":"2026-07-22T22:54:32Z","closed_at":"2026-07-22T23:00:16Z","close_reason":"merged 0e12b0a281 into master via aip gate","labels":["awaiting-gate","epic:taskflowtest-6qf","guarded-changes","size:L"],"dependency_count":0,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"taskflowtest-6qf","title":"Replace TaskFlow with Move: a retro installable PWA exercise-reminder assistant","description":"# Requirements: Move — a sit-less exercise-break reminder (retro PWA)\n\n## Summary\nA no-backend Progressive Web App that periodically reminds the user (every configurable X minutes)\nto get up and do a short exercise, delivered by a **Clippy-style retro assistant**. Exercises are\nchosen from a tag-driven catalog to suit the user's equipment and current context. Users build\n**loadouts** (named, shareable filter configs), track progress over time, and can export/import\nloadouts as self-contained JSON. Replaces the existing TaskFlow app in this repo, reusing its\nscaffolding. All state persists locally and survives reloads — export is only for sharing.\n\n## Context \u0026 reasoning\nInspired by neuroscientist Erik Scherder's argument that prolonged sitting harms the brain and\nthat regular movement supports cognition. The goal is to nudge desk-bound people to break up\nsitting with brief, context-appropriate exercise, in a fun, deliberately kitschy late-'90s Windows\npackage. Success = a person opens the site, a sensible default works in seconds, and they get\nwell-timed, doable reminders that fit their situation and keep them coming back.\n\n## Goal\nShip a usable, installable retro PWA that fires context-aware exercise reminders on a configurable\ninterval via an assistant character, with a tag-driven catalog, shareable loadouts, and honest\nprogress tracking — all client-side, no backend, auto-persisting.\n\n## Functional requirements\n- **Reminders:** configurable interval (\"every X minutes\"). A reminder fires a browser\n  notification while the app is open (foreground or backgrounded); it stops when the app is fully\n  closed. Reminders **start paused** on first visit — the user opts in via a clear Start control,\n  after the notification-permission flow.\n- **Assistant persona:** reminders are delivered by a **Clippy-style assistant character** in a\n  retro speech bubble (e.g. \"It looks like you've been sitting a while. Fancy some squats?\"),\n  matching the design reference. This persona is a defining part of the UX, not just chrome.\n- **Rich notifications:** the OS notification should, where the browser supports it (service-worker\n  `showNotification` with actions/image), show the exercise image and inline **Done / Skip /\n  Snooze / Shuffle** action buttons. **Graceful fallback:** where actions/images aren't supported,\n  show a brief notification that opens the app to the same actions. Never depend on inline actions\n  working everywhere.\n- **Reminder interaction:** Mark done / Skip / Snooze (remind again shortly) / Shuffle (swap for\n  another eligible exercise) — available both in-notification (where supported) and in-app.\n- **Reminder cue:** play a short **retro Windows-style \"ding\"** sound when a reminder fires. (No\n  vibration.)\n- **Exercise catalog:** a built-in curated set, **plus** users can add their own custom exercises.\n- **Exercise data model:** each exercise has a name, **form/how-to instructions** (how to do it\n  properly), an **image** (ship a generated placeholder per exercise; the user will swap in real\n  images/GIFs later — so the model references a swappable image asset), a **structured target**\n  (reps, e.g. \"12 reps\", or time, e.g. \"hold 30s\") shown prominently on the card and in the\n  notification, and **tags** (below).\n- **Unified tag system:** every exercise carries free-form, **user-extendable tags**. Tags cover\n  ALL axes — equipment (`pull-up-bar`, `resistance-bands`…), context/location (`office`, `home`…),\n  duration/time budget, intensity, body-part/type, and anything users invent. New tags can be\n  created on the fly. (User's explicit steer: one scalable tag model, not fixed fields.)\n- **Location/context:** a **manual context selector** (e.g. Office / Home / Outdoors) — no GPS. The\n  active context constrains eligible exercises.\n- **Equipment:** the user records which equipment they have; exercises whose required-equipment\n  tags aren't satisfied are excluded from the active pool.\n- **Loadouts:** a **library of named loadouts** (e.g. \"Office\", \"Home gym\", \"Travel\"); one active\n  at a time, user-switchable. A loadout is a set of tag-based rules selecting the eligible pool.\n  Filtering must support **include / require-all / exclude** semantics so \"requires pull-up bar\"\n  and \"office-safe / never show jumping\" both work.\n- **Sensible default:** first-time visitors get a ready-to-use default loadout (no equipment,\n  office-safe) so they can start immediately without configuring anything.\n- **Export / import:** a loadout exports to a **self-contained JSON bundle** — the loadout PLUS any\n  custom exercises it references, embedded — so it works on import even if the recipient lacks\n  those. Import merges the bundle in (loadout added to library, custom exercises added to catalog,\n  no duplicate clobbering). Export is the ONLY thing that leaves the device, and only on explicit\n  user action.\n- **Auto-persistence:** ALL state — settings, interval, equipment, context, loadouts, custom\n  exercises, tags, progress/history — saves locally (localStorage) automatically and survives\n  reloads. The app must NOT reset to a fresh state on refresh; the user never has to export to keep\n  their setup.\n- **Progress tracking:** persist **full history + day streaks**, a done count, and an\n  **ignored/skipped count**. Plus a **motivational stat** framed **honestly** — grounded in real,\n  citable movement-break / sedentary-behaviour research (e.g. active minutes reclaimed, sitting\n  interrupted). Any brain/cognitive framing is an explicitly playful, clearly labelled estimate —\n  never an invented number presented as peer-reviewed fact.\n- **PWA:** installable, with a web app manifest and a service worker; the static app works offline\n  once loaded. (The service worker also backs rich notifications.)\n\n## Technical direction (user's steering)\n- **Replace TaskFlow** in this repo, reusing the scaffolding: Vite + React 19 + TS, Tailwind v4\n  (CSS-first, already wired), the `src/storage.ts` load/save-with-type-guard-validation pattern,\n  the `useBoard`-style \"one hook owns state + auto-persist via `useEffect`\" pattern, the `types.ts`\n  `type` + union-literal modelling, named-function components with local `XProps`, and the Vitest +\n  Testing Library setup (`jsdom`, `localStorage.clear()` in `beforeEach`, role/label queries).\n  TaskFlow's board/columns/card logic and tests are removed.\n- **Visual style follows `ai-design/frontendstyle.jpg`** — a deliberately retro late-'90s/early-2000s\n  **Windows / MS Office** aesthetic (Clippy-era): beveled 3D buttons, classic gray window chrome,\n  yellow speech-bubble balloons, pixel/serif type. Intentionally \"not a good frontend\" but that\n  exact style. (Consult the design reference file directly.)\n- **One unified, user-extendable tag system**; loadouts filter with include/require-all/exclude.\n- **No backend** — static site; reminders are a client timer + service worker; no push server.\n- **Manual context**, not GPS.\n\n## Interactions \u0026 dependencies\n- **Removes** TaskFlow domain code: `src/board.ts`, `src/hooks/useBoard.ts`, `src/components/*`,\n  `src/board.test.ts`, and the board-specific parts of `src/App.tsx` / `src/App.test.tsx`.\n  **Reuses** the *patterns* in `storage.ts`, the hook, `types.ts`, `index.css` (Tailwind v4 + CSS\n  variables), and `src/test/setup.ts`.\n- **Reminder engine ↔ notifications ↔ service worker ↔ PWA:** the interval timer, notification\n  permission, rich-notification actions, and the SW are interdependent; the SW + manifest are new\n  PWA infrastructure layered onto the Vite config. Rich-notification actions specifically require\n  the service worker.\n- **Assistant persona ↔ theme ↔ design reference:** the Clippy-style character and speech-bubble UI\n  derive from `ai-design/frontendstyle.jpg`.\n- **Tags are the backbone:** catalog, custom exercises, equipment, context, and loadout filtering\n  all key off the same tag model — designed once, shared.\n- **Export/import ↔ custom exercises ↔ loadouts:** a shared bundle must embed referenced custom\n  exercises; import must merge without duplicating or clobbering.\n- **Progress tracking ↔ reminder actions:** done/skip/snooze/shuffle outcomes feed history,\n  streaks, done and ignored counts, and the motivational stat.\n\n## Acceptance criteria\n- Fresh visitor: app loads with the default loadout, reminders **paused**, a visible Start control,\n  in the retro theme.\n- After granting permission and starting, a reminder fires after the interval, delivered by the\n  assistant, showing an eligible exercise (respecting active loadout + context + owned equipment)\n  with its target, instructions, and (placeholder) image; a retro ding plays.\n- Rich notification shows inline Done/Skip/Snooze/Shuffle where supported; where not, a brief\n  notification opens the app to the same actions. Each action behaves correctly and updates tracking.\n- A user can create a custom exercise (name, instructions, target, image, tags), mint a new tag,\n  and see it become eligible when a loadout's filters match.\n- Switching the active loadout changes which exercises can appear.\n- Exporting a loadout yields JSON containing the loadout + its referenced custom exercises;\n  importing on a fresh profile reproduces and runs them.\n- Reload does NOT reset state: settings, loadouts, custom exercises, and progress all persist.\n- History, streak, done count, and ignored count persist; the motivational stat renders with honest\n  framing/citation.\n- App is installable as a PWA and loads offline after first load.\n- Quality gates pass: `npm run lint` (oxlint), `npm run build` (tsc + vite), new Vitest tests.\n\n## Constraints\n- **No backend / static site**; all data local; no accounts, no sync.\n- **Privacy:** no geolocation; context is manual; nothing leaves the device except user-initiated\n  JSON export.\n- **Honesty:** no fabricated statistic presented as science; the motivational stat cites real\n  research or is clearly labelled a playful estimate.\n- **Notifications** require user permission and only fire while the app runs; rich actions need the\n  service worker and degrade gracefully.\n- **Stack:** React 19 + TS + Vite + Tailwind v4 (CSS-first); follow existing repo conventions.\n- **Design:** match `ai-design/frontendstyle.jpg`; respect `paths.forbidden` and worker/gate rules.\n\n## Out of scope\n- Any backend, server, database, or cloud sync.\n- True notify-when-fully-closed push (needs a push server) — explicitly excluded.\n- GPS/automatic location detection.\n- Multi-user accounts / auth.\n- Native mobile apps (web PWA only).\n- Real exercise images/GIFs — placeholders ship now; the user adds real assets later.\n\n## Open decisions / assumptions (for the planner to resolve)\n- Default reminder interval value (assume ~30 min); snooze duration (assume ~5 min).\n- Exercise selection from the eligible pool (assume random with avoid-immediate-repeat).\n- Exact motivational-stat metric + cited source/wording (must satisfy the honesty constraint).\n- App name/branding (working title \"Move\"; final name TBD).\n- PWA offline scope (assume app-shell caching of the static build).\n- Placeholder image format/look (assume a simple generated placeholder per exercise; real GIFs later).\n- Exact reps-vs-time target representation (assume one field that expresses either).","status":"open","priority":2,"issue_type":"epic","owner":"wouter.molemaker@simac-ids.com","created_at":"2026-07-22T22:52:37Z","created_by":"Wouter Molemaker","updated_at":"2026-07-22T22:52:37Z","dependency_count":0,"dependent_count":0,"comment_count":0}
